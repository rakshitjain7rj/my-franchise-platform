# Storefront public env (safe to expose to the browser).
# Keep delivery NEXT_PUBLIC_* in sync with apps/backend DELIVERY_* on deploy.

# NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
# NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
# NEXT_PUBLIC_DEFAULT_FRANCHISE_ID=

# Delivery policy strip + free-over progress (charge path uses backend env only).
# NEXT_PUBLIC_DELIVERY_FREE_MILES=1
# NEXT_PUBLIC_DELIVERY_PER_MILE=4.49
# NEXT_PUBLIC_DELIVERY_DEFAULT_RADIUS_MI=10
# NEXT_PUBLIC_DELIVERY_FREE_OVER_GBP=150
